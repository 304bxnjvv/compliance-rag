# Claves ficticias para que el contexto de Spring cargue en los tests
# sin necesidad de claves reales (los tests no llaman a las APIs externas).
spring.ai.deepseek.api-key=test-dummy-key
spring.ai.google.genai.embedding.api-key=test-dummy-key
